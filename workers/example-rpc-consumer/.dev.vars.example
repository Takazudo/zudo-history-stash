# Keep real values only in ignored .dev.vars or .dev.vars.preview files. The trigger bearer-gates
# every executable example route before the internal Stash write token can be used.
STASH_TOKEN=
RPC_SMOKE_TRIGGER_TOKEN=
MULTIPART_SMOKE_STASH=
