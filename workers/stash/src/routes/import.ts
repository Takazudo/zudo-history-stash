import { Hono } from "hono";
import type { AppEnv } from "../context.js";

/** Placeholder for the fenced history import route. */
const importRoutes = new Hono<AppEnv>();

export default importRoutes;
