# Design reference for epic #76 (Diff View)

Static prototype of the diff page: open `04-diff-page-split-upgrade/index.html` in a browser (it links `../shared/tokens.css` and `../shared/base.css`). Dark by default; use the header toggle for light. A layout/copy reference only — the production implementation follows the epic's sub-issues. The confirm sub-issue deletes this directory before the epic PR merges.
