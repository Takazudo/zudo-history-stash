# Design reference for epic #85 (Viewer Consumer)

Static prototype of the edit workbench the owner chose: open `02-edit-workbench-live-diff/index.html` in a browser (it links `../shared/tokens.css` and `../shared/base.css`). Dark by default; header toggle for light. A layout/copy reference only — the epic's sub-issues are the spec (in particular the four-state workbench model and the idempotency rules, which the prototype simplifies). The confirm sub-issue deletes this directory before the epic PR merges.
